import './index.scss';

const Button = (buttonText) => {
  const button = document.createElement('button');
  button.type = button;
  button.textContent = buttonText;

  return button;
};

export { Button };
